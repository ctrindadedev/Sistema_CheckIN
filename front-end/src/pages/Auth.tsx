//todo rota de login (usar zod para validar as informações?)

import React from "react";

const auth = () => {
  return (
    <div>
      Login aqui. Sucesso? redireciona para page evento-user, falha? informa o
      erro
    </div>
  );
};

export default auth;
